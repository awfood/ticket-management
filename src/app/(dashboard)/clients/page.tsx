import { ClientListClient } from '@/components/clients/client-list'

export const metadata = {
  title: 'Clientes - AWFood Suporte',
}

export default function ClientsPage() {
  return <ClientListClient />
}
